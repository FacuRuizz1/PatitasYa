export enum PostType {
  PERDIDA = 'PERDIDA',
  ENCONTRADA = 'ENCONTRADA',
  ADOPCION = 'ADOPCION'
}

export enum PostStatus {
  ACTIVA = 'ACTIVA',
  RESUELTA = 'RESUELTA'
}

export enum Rol {
  USER = 'USER',
  ADMIN = 'ADMIN'
}